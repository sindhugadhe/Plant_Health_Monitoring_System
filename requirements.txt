click==8.0.4                # Supports command-line operations for Flask.
gunicorn==20.1.0            # Used to deploy/run Flask app on server.
itsdangerous==2.0.1           # Helps with secure login sessions and tokens.
Jinja2==3.0.3                # Used for HTML templates (index.html, register.html).
MarkupSafe==2.0.1            # Protects HTML content from unsafe data.
tensorflow==2.8.0
flask==2.0.2                # Used to create the web application. It connects frontend and backend.
werkzeug==2.0.2              # Internal Flask utility for routing, requests, debugging.
pandas==1.3.5
keras_preprocessing==1.1.2
opencv-python==4.5.3.56      # Reads plant leaf images, resizes, processes.
opencv-contrib-python==4.5.3.56 # Extra OpenCV features.
opencv-python-headless==4.5.3.56 # OpenCV without GUI (for servers).
opencv-contrib-python-headless==4.5.3.56 # Same with extra modules and no GUI.
numpy==1.21.6
tensorflow-cpu==2.8.0
tensorboard==2.8.0
flatbuffers==1.12.0        # Needed internally by TensorFlow.
protobuf==3.19.1           # Used for TensorFlow compatibility and data serialization.
Pillow==8.4.0              # Used to open, edit, convert images